spring:
  thymeleaf:
    cache: false
    check-template-location: true
    prefix: classpath:/templates/
    suffix: .html

  profiles:
    include: oauth

  datasource:
    url: localhost:3306/aim?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: kiziri
    password: Ddnwlsdl23@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace