server:
  address: localhost
  port: 8080

spring:
  thymeleaf:
    cache: false
    check-template-location: true
    prefix: classpath:/templates/
    suffix: .html

  profiles:
    include: oauth

  datasource:
    url: jdbc:mysql://localhost:3306/aim?useSSL=false&characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: kiziri
    password: Ddnwlsdl23@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        #show_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace